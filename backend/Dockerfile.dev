# Development Dockerfile with hot reload
FROM node:20-alpine

WORKDIR /app

# Install dependencies (including tsx for hot reload)
RUN npm install -g tsx nodemon

# Copy package files
COPY package*.json ./
RUN npm install

# Copy source
COPY . .

# Expose port
EXPOSE 3000

# Development with hot reload
CMD ["npm", "run", "dev"]
